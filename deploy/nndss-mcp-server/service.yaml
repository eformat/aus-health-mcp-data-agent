apiVersion: v1
kind: Service
metadata:
  name: nndss-mcp-server
  labels:
    app.kubernetes.io/name: nndss-mcp-server
spec:
  ports:
    - port: 9090
      protocol: TCP
      name: http
      targetPort: 9090
  selector:
    app.kubernetes.io/name: nndss-mcp-server
  type: ClusterIP
