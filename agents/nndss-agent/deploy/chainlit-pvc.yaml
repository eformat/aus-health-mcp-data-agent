apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chainlit-data
  labels:
    app.kubernetes.io/name: nndss-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
